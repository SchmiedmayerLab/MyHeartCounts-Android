#
# This source file is part of the My Heart Counts Android open-source project
#
# SPDX-FileCopyrightText: 2026 Stanford University and the project authors (see CONTRIBUTORS.md)
#
# SPDX-License-Identifier: MIT
#

coverage:
  status:
    # Never let overall coverage slip. The application module is at roughly 22% now that the Grove
    # modules and their tests report in their own repository.
    project:
      default:
        target: auto
        threshold: 0.5%
    # New code is held above the current project average so coverage climbs.
    patch:
      default:
        target: 45%

comment: false
